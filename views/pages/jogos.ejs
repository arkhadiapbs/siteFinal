<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Jogos - Arkhadia</title>
  <link rel="stylesheet" href="/css/estilo.css">
</head>
<body>
<header>
  <h2>🎮 Arkhadia</h2>
  <nav>
    <a href="/">Início</a>
    <a href="/jogos">Jogos</a>
    <a href="/marketplace">Marketplace</a>
    <a href="/comunidade">Comunidade</a>
  </nav>
</header>

<main>
  <h1>Lista de Jogos</h1>

  <section>
    <h2>Top 10 do Ano</h2>
    <ul>
      <% top10Ano.forEach(jogo => { %>
        <li><%= jogo %></li>
      <% }) %>
    </ul>
  </section>

  <section>
    <h2>Mais Jogados</h2>
    <ul>
      <% maisJogados.forEach(jogo => { %>
        <li><%= jogo %></li>
      <% }) %>
    </ul>
  </section>

  <!-- NOVA SEÇÃO: VOTAÇÃO -->
  <section>
    <h2>🗳️ Melhor Jogo do Mês</h2>
    <p>Clique no botão para votar em um dos 3 jogos candidatos!</p>
    <button onclick="abrirVotacao()">Votar</button>
  </section>

  <!-- MODAL DE VOTAÇÃO -->
  <div id="votacaoModal" style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background-color:rgba(0,0,0,0.6); display:flex; align-items:center; justify-content:center;">
    <div style="background:#1b263b; padding:20px; border-radius:10px; color:#e0e1dd; width:300px;">
      <h3>Vote no Melhor Jogo</h3>
      <form id="formVotacao">
        <% candidatos.forEach(jogo => { %>
          <div>
            <input type="radio" name="jogo" value="<%= jogo %>" required> <%= jogo %>
          </div>
        <% }) %>
        <div>
          <textarea name="justificativa" placeholder="Por que gostou do jogo?" required style="width:100%; margin-top:10px;"></textarea>
        </div>
        <button type="submit" style="margin-top:10px;">Enviar Voto</button>
        <button type="button" onclick="fecharVotacao()" style="margin-top:10px;">Cancelar</button>
      </form>
    </div>
  </div>

</main>

<footer>© 2025 Arkhadia - Todos os direitos reservados</footer>

<script>
  function abrirVotacao() {
    document.getElementById('votacaoModal').style.display = 'flex';
  }

  function fecharVotacao() {
    document.getElementById('votacaoModal').style.display = 'none';
  }

  document.getElementById('formVotacao').addEventListener('submit', async function(e) {
    e.preventDefault();
    const formData = new FormData(this);

    const data = {
      jogo: formData.get('jogo'),
      justificativa: formData.get('justificativa')
    };

    try {
      const res = await fetch('/api/voto', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
      });

      const result = await res.json();
      alert(result.message);

      if (res.ok) {
        fecharVotacao();
      }

    } catch (err) {
      alert("Erro ao enviar voto.");
    }
  });
</script>
</body>
</html>
